package automatons

// Helper function for max
func max(a, b rune) rune {
	if a > b {
		return a
	}
	return b
}

// Helper function for min
func min(a, b rune) rune {
	if a < b {
		return a
	}
	return b
}
